import { SecurityTokenDevice } from './security-token-device';

describe('SecurityTokenDevice', () => {
  it('should create an instance', () => {
    expect(new SecurityTokenDevice()).toBeTruthy();
  });
});
